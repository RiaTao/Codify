Hello world from the terminal \
Hello world from GitHub

## HTML Page
This repository includes an `index.html` file that displays "HELLO WORLD" in all caps.

To view the HTML page:
1. Open the `index.html` file in a web browser
2. You should see "HELLO WORLD" displayed on the page

## Testing
A simple test file `test.html` is included to verify that the index.html file contains "HELLO WORLD" in all caps.

To run the test:
1. Open the `test.html` file in a web browser
2. The test results will be displayed on the page
